In order to compile the program:
	run 
		* ant (just enter ant into the terminal)

	- The program will then compile the files into the bin folder


In order to run the files:
	run
		* java -cp bin scheduler.Scheduler inputs/input-1.txt

	- You may also run the files in verbose mode
		* java -cp bin scheduler.Scheduler inputs/input-1.txt --verbose